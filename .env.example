# Database URL - Replace with your PostgreSQL connection string
# For Supabase: Get the connection string from your Supabase project settings
DATABASE_URL="postgresql://postgres:password@localhost:5432/nihongo?schema=public"

# NextAuth Secret - Generate with: openssl rand -base64 32
AUTH_SECRET="generate-a-secret-with-openssl-rand-base64-32"

# NextAuth URL (for production, use your actual domain)
NEXTAUTH_URL="http://localhost:3000"
